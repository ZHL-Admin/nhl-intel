# Rebuild P0 — Preconditions

Production rebuild of the shift backbone + model retrains. Branch:
**`rebuild/dedup-segments-retrain`** (off `redesign/the-sheet` @ `196f672`).
`research/deployment-atlas/` is read-only reference throughout.

## Nightly shift fallback — status
The HTML shift fallback + `check_shift_coverage` monitor (Workstream A) are in the
working tree but **uncommitted and not deployed**:
- `dags/nhl_daily.py` — modified (fallback block + monitor task; `check_shift_coverage`
  defined, 6 references)
- `ingestion/loaders.py` — modified (`delete_rows_by_game_id`)
- `ingestion/shift_report_parser.py` — untracked (new)

**The fallback has NOT had a live nightly run** (not committed/deployed to the
scheduler). `check_shift_coverage` therefore has **no live result to report**. Per
the work order, proceeding anyway — the fallback only affects tonight-forward data;
the 563 historical empty-shift games were already backfilled into
`raw_shift_charts` on 2026-07-10, which is what this rebuild consumes.

## Atlas freeze reference (read-only validation baseline)
- Repo: last commit `196f672` (2026-07-07); working tree carries the 2026-07-10
  session work (research build + Workstream A), uncommitted.
- Atlas corpus Parquet materialized **2026-07-10** — `stints.parquet` 16:45,
  `rapm_variant.parquet` 18:01. This is the frozen reference for all P1/P3
  reconciliations. It will not be modified.

## Tooling
- dbt (BigQuery, `~/.dbt/profiles.yml`, SA `secrets/nhl-intel-sa.json`) — available.
- Target warehouse: `nhl-intel-498216` (`nhl_raw` / `nhl_staging` / `nhl_models`).

## Plan (gates)
- **P1** dbt rebuild (dedup → segments w/ goal cuts → context/on-ice-events → marts →
  scheduling) + reconcile vs Atlas stints. **STOP.**
- **P2+P3** ingestion stragglers + model retrains (player_impact, shot_xg). **STOP.**
- **P4** consumer sweep + ratings-diff dossier. **STOP.**

Proceeding to P1.
