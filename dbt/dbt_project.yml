name: 'nhl_intel'
version: '1.0.0'
config-version: 2

profile: 'nhl_intel'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"
  - "logs"

models:
  nhl_intel:
    raw:
      +materialized: view
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    mart:
      +materialized: table
