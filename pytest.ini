[pytest]
# Canonical test suite lives in tests/. The repo-root test_*.py files are
# live-service integration smoke scripts (NHL API, BigQuery, Gemini) that are run
# on demand, not part of `make test`.
testpaths = tests
markers =
    network: test hits live NHL endpoints; skips cleanly when offline.
